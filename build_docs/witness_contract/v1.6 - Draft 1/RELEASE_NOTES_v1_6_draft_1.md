# Release Notes — v1.6 Draft 1 Full Upgrade

This package corrects the earlier compact v1.6 draft by preserving all v1.5 Draft 2 corpus coverage. It adds a universal backend upgrade patch, v1.5-to-v1.6 path map, SRNN source review integration, and Markdown replacements for non-Markdown artifacts.

## Counts

- v1.5 manifest artifacts covered: 55.
- Markdown files generated in this package: 75.

## Notes

The package is Markdown-only. Binary diagrams and JSON schema files are represented as Markdown provenance/schema appendices; machine-readable regeneration is a build step.
