# Duotronic Witness Contract v1.6 — Draft 5.3.5

**Status:** completed standalone corrective development draft; permanently not frozen.  
**Authority defaults:** theorem disabled; promotion disabled; release false.  
**Source provenance:** `unpublished_workspace`; activation blocked pending a clean committed source build.

Draft 5.3.5 corrects OCI domain dispatch, exact sandbox-control recording,
policy-decision authority binding, inspector build targeting, runtime identity,
non-root execution, hostile-source resource bounds, bounded process output,
schema lifecycle governance, synchronous API conformance, recursive manifest
metadata, and package provenance. All earlier corpus material and exact Draft
5.2.2 through 5.3.4 archives are retained.

Start with `START_HERE.md`, then run:

```bash
python3 executable/validators/validate_draft5_3_5_corpus.py
```

Portable validation proves internal corpus closure only. Strict Lean, strict
TLC, governed-image execution and inspection, reproducible build attestations,
committed-source provenance, and an external governance signature remain
activation evidence. Their absence cannot be converted into authority by a
portable green report.
