# Draft 5.3.8 Update Checklist

- [x] Treat completed cache rows as untrusted and store a signed cache envelope.
- [x] Re-resolve current policy and verify compiler-witness signature, signer authorization, payload digest, outer status, and all request/principal/content bindings on every cache hit.
- [x] Secure the idempotency database path, parent, ownership, modes, symlinks, link counts, WAL, and SHM files.
- [x] Require measured evidence for `environment_allowlist` and `explicit_entrypoint`.
- [x] Compare the complete actual environment-key set with literal and governed runtime-created keys; reject all undeclared keys.
- [x] Separate semantic witness identity from exact execution-evidence identity while retaining signed argv, mount, path, runtime, and timestamp evidence.
- [x] Explicitly close every SQLite connection on success, rollback, and exception paths.
- [x] Bind the request to a verified middleware principal and forbid caller-controlled subject fields.
- [x] Use structured `unittest.TestResult` counts and independent warning detection; declare Python 3.12 and 3.13 support.
- [x] Bound global and per-principal in-flight rows, total rows, completed rows, database bytes, and expired leases; add the state/lease index.
- [x] Refresh runtime documentation, schemas, fixtures, OpenAPI, tests, registry, and release metadata.
- [x] Merge all changes into one Draft 5.3.8 corpus and omit embedded historical source-package ZIPs.
- [x] Preserve permanent-not-frozen lifecycle and keep theorem, promotion, and release authority disabled behind all eight external gates.
