# Release Notes — v1.6 Draft 5.3.1

Draft 5.3.1 is a complete corrective development corpus and is not frozen.

## Closed defects

| Prior defect | 5.3.1 closure |
|---|---|
| Self-attested proof authority | Content-bound, signed, server-produced v2 witness chain |
| Unrelated or denied non-collapse path accepted | Exact claim/policy/proof match and relevant allowed `conjectural -> theorem` transition |
| Mutable authority rows | Update/delete denial triggers plus supersession records |
| Canonical resolver booted 5.2 | Single 5.3.1 descriptor and corrected resolver |
| Non-collapse schema counterexamples | V2 schema forbids protected identity collapse and requires proof on allowed proof upgrades |
| Validator skipped missing dependencies | Required-phase fail-closed runner and explicit report |
| Vacuous replay pass | Nonempty grammar/manifest, satisfied required assumptions, all-pass instruction results |
| Client could POST conclusions | Request-oriented authenticated API; witness/gate resources are read-only |
| Integer matched two `oneOf` branches | V2 claim object uses disjoint integer/number branches |
| Abstract formal models overstated | Assurance boundary is explicit; strict tool evidence remains required |
| No executable upgrade migration | New v2 tables and legacy quarantine migration |
| Placeholder license | Explicit all-rights-reserved notice |
| No external trust root | Explicit unsigned state and future trust-root policy |

## Added mathematics profile

The positive-baseline polygonal framework v1.2 is included unchanged as a source document and connected to Witness Contract semantics by a new integration profile, schema, reference evaluator, fixtures, and tests. The integration preserves the key distinction between payload, codeword, status, and physical signal.

## Compatibility

All Draft 5.2.2 files and the exact source ZIP are retained. V1 records are replay-readable. New authoritative writes use v2 object generations and the 5.3.1 SQL tables. No legacy proof record is promoted automatically.
