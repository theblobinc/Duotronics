# Duotronics v1.6 — Draft 5.3.12 Update Checklist

## P0 — Complete-validator reliability

### 1. Fix the warning-free phase stall

- Reproduce the monolithic validator under Python 3.13.5.
- Replace unbounded post-timeout `communicate()` with bounded reaping.
- Close captured pipes after process-group termination.
- Capture child output in bounded temporary files.
- Enumerate and kill surviving descendants.
- Record nested substage progress.
- Run the complete validator repeatedly from another isolated parent.

Required tests:

- full validator completes 10 consecutive times on Python 3.12;
- full validator completes 10 consecutive times on Python 3.13;
- intentional nested hangs cannot retain pipes or descendants;
- warning-free regression phase completes when invoked directly and through the phase worker.

## P1 — Authenticate stale cache rows before rotation classification

### 2. Preserve historical verification authority

Choose one:

- a governed historical cache-key verification registry; or
- signed registry-lineage snapshots retained for replay.

Replay order must be:

1. canonical parse;
2. schema validation;
3. signer lookup in current or governed historical keys;
4. signature verification;
5. registry/rotation-lineage verification;
6. stale-row evidence;
7. stable `409`.

Unknown or unauthenticated signers must produce an integrity error, not a rotation-specific result.

Required tests:

- authenticated predecessor row returns `409`;
- forged unknown-signer row returns integrity failure;
- altered stale row cannot produce rotation evidence;
- revoked historical signer follows the governed replay policy.

## P2 — Production-shape loader integration

### 3. Run the actual non-root loaders

- Use the intended service UID/GID.
- Use root-owned immutable system ancestors.
- Use a service-owned mode-0700 trust root.
- Load real canonical configuration, schemas, keys, and signed registries.
- Verify both production loader functions complete.
- Retain root-execution rejection.
- Test symlink, writable ancestor, wrong UID, and replacement races.

## P3 — External gates

### 4. Strict Lean
### 5. Strict TLC
### 6. Governed hermetic OCI execution
### 7. Signed OCI image attestation
### 8. Signed verifier executable attestation
### 9. Reproducible inspector build attestation
### 10. Clean committed-source provenance
### 11. External governance authorization

## Draft 5.3.12 acceptance criteria

- Full validator completes repeatedly on both Python lines.
- No descendant or pipe leak remains after timeouts.
- Stale cache rows are authenticated before rotation classification.
- Both production loaders succeed under the real non-root service identity.
- Authority remains disabled unless all eight external gates independently pass.
