# Migration: 5.3.17 to 5.3.18

This migration is additive and fail-closed.

1. Import the 5.3.18 schemas and relation registry without activating authority.
2. Create distinct sandbox and production authority-domain records.
3. Backfill existing evidence as `legacy-unclassified`; it is not activation-eligible until explicitly re-attested.
4. Split any combined probe/recheck record into an immutable original measurement and a linked revalidation.
5. Assign a stability class and comparison policy to every registered probe.
6. Build typed evidence edges. Inferred graph proximity is not migrated as proof.
7. Recompute graph roots and checkpoint segments with the 5.3.18 domain labels.
8. Run the full VM harness against the migrated development corpus.
9. Keep production disconnected. A production release requires new production evidence and explicit operator authorization.

Rollback removes only 5.3.18-derived rows and selectors. It must not delete inherited evidence or history.
