SPECIFICATION Spec
\* Deliberately small exhaustive instance: one claim still exercises unary and
\* binary (self-pair) composition while keeping the strict activation gate finite.
CONSTANTS
  Claims = {c1}
  Policies = {p1}
  MaxClaims = 1
INVARIANTS
  TypeOk
  CompositionHasWitness
  TheoremPromotionHasProof
